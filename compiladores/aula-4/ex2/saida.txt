/* Um programa para calcular o mdc
   segundo o algoritmo de Euclides. */

INT gcd (INT u, INT v)
{       
	IF (v == 0) RETURN u ;
	ELSE RETURN gcd(v,u-u/v*v);
	/* u-u/v*v == u mod v */
}

VOID main(VOID)
{	INT x; INT y;
	x = input(); y = input();
	output(gcd(x,y));
}
